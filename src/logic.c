#include "logic.h"
#include "globals.h"

void execute_logic_operation() {
    // Executa a operação lógica correspondente a opção selecionada
    switch (opcao_atual) {
        case 0: // AND
            // Faz alguma coisa
            break;
        case 1: // OR
            // Faz outra coisa
            break;
        case 2: // NOT
            // Faz outra coisa
            break;
        case 3: // NAND
            // Faz outra coisa
            break;
        case 4: // NOR
           // Faz outra coisa
            break;
        case 5: // XOR
           // Faz outra coisa
            break;
        case 6: // XNOR
            // Faz outra coisa
            break;
    }
}
